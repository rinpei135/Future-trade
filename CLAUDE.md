# 30秒先が見えるトレード（Future-trade）— Claude Code 引き継ぎ書

claude.ai のチャットで開発してきた Web ゲームの引き継ぎ資料です。まずこのファイルを読み、
「5. 未完了タスク」から作業を進めてください。やり取りはすべて日本語で行ってください。

## 1. 概要

- **コンセプト**：「30秒先の未来が見えたら、トレードで勝てるのか？」を体験するジョーク系トレードゲーム。
  チャート・注文・損益は「30秒遅れの価格（＝みんなに見えている現在）」で動き、
  「未来視点」をONにしたときだけ、チャート上に30秒先の値動き（破線）が見える。
- **公開URL**：https://rinpei135.github.io/Future-trade/ （GitHub Pages、リポジトリ `rinpei135/Future-trade`、`main` の `/ (root)`）
- **ランキング**：Firebase（プロジェクトID `future-vision-trade`）の Firestore ＋ 匿名ログイン。無料の Spark プラン（カード登録なし）で運用する方針。
- **利用者の前提**：組み込み系エンジニア。スマホから作業することも多い。完全無料の構成を希望。

## 2. ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | 画面（HTML/CSS）。CSP を `<meta>` で設定。**インラインの `<script>` やイベント属性（onclick 等）は書かない**（CSP で禁止している） |
| `app.js` | ゲーム本体（相場生成・取引・チャート描画・ラウンド・挑戦状・シェア・検証など） |
| `ranking.js` | Firebase 連携（ES Module）。`window.Ranking` に `submitBest / fetchTop / myId / EQUITY_CAP` を公開 |
| `privacy.html` | プライバシーポリシー・利用上の注意 |
| `manifest.webmanifest`・`icon-*.png`・`apple-touch-icon.png` | ホーム画面に追加（PWA）用 |
| `ogp.png` | X 等のリンクプレビュー画像（1200×630） |
| `firestore.rules` | Firestore のセキュリティルール（Firebase コンソールに貼って公開する） |
| `tests/` | 自動テスト一式（→ 6. テスト） |
| `tests/rules/` | Firestore ルールの単体テストと、本物の `ranking.js` をエミュレーターで動かすテスト（Node.js＋Java が必要） |

## 3. 主な仕様（数値）

- 軍資金 **100万円**、ランキングに登録できる資産の上限 **1,000万円**（`ranking.js` の `EQUITY_CAP` と `firestore.rules` の値を必ず同期）
- 未来視点：連続 **30秒** 使用 → **60秒** クールダウン（5分で最大4回）
- 価格更新 200ms（`TICK_MS`）、遅延 30秒（`DELAY_MS`）、起動時に過去8時間分を生成（`BACKFILL_MS`）
- 銘柄（どちらも架空）
  - **CRN/JPY（クロノ/日本円）**：開始 88.42円、レバレッジ25倍、スプレッド0.6銭、数量 1万〜20万通貨（1,000通貨単位）、グリッド0.020円刻み
  - **TKWP（トキワープHD）**：開始 482円、レバレッジ3.3倍、値幅制限 ±80円（ストップ高・安で反対売買が約定しない）、数量 1,000〜5,000株（100株単位）、材料ニュースで乱高下
- モード
  - **フリープレイ**：時間無制限。ランキング・挑戦状の対象外
  - **5分タイムアタック**：新しいランダム相場で5分勝負。終了時に自動全決済。**ランキング登録はここからのみ**
  - **挑戦状**：タイムアタックの結果から作ったURLで、同じ相場・同じ5分で勝負。ランキング対象外

## 4. 設計の要点（変更時に壊しやすい所）

- **相場の再現性**：乱数は `mulberry32(seed ^ SYM_SALT[k])`。価格は「相場時刻 m」（ラウンド開始＝0、200ms刻みのグリッド、`GRID_START` から生成）ごとに `stepPrice()` で決定的に進む。
  **`stepPrice()` の計算式や乱数の呼び出し順を変えると、過去の挑戦状URL・ランキングの検証がすべて合わなくなる**。変える場合は挑戦状の形式バージョン（`v`）を上げること。
- **表の現在値**：注文・損益・スプレッドは `engines[k].pub`（= 30秒前の価格。`updatePub()` で毎回更新）と、それを元にした `bidOf(k)` / `askOf(k)` を使う。本物の最新価格はチャートの未来線にしか出さない（未来の情報を注文パネルに出さない方針）。
- **スリッページ**：運ではなく、その瞬間のスプレッドの広がり具合だけで決まる（挑戦状の公平性のため）。
- **ラウンド終了**：`finishRoundIfDue()` が「終了時刻ちょうど（m = 5分）」の相場で締める。タブを裏に回して遅れて戻っても、数分後の価格で決済されない。
- **挑戦状URL**：`<公開URL>#c=<base64url(JSON)>`。JSON は `{v:3, s:シード, a:開始m, b:a+300000, n:名前, e:最終資産, t:取引数}`。受け取り側は `v === 3` かつ5分ちょうどのものだけ受け付ける。
- **ランキング（Firestore）**：ドキュメントID ＝ 匿名ログインの uid（1人1件、自己ベストのみ更新、60秒の連続登録制限）。コレクションは
  - 全期間 `ta5`
  - 今日 `ta5d_{年}_{月}_{日}`（日本時間、ゼロ埋めなし）
  - 今週 `ta5w_{floor((UNIXミリ秒 + 9時間 + 3日) / 7日)}`
  - クライアント（`ranking.js` の `periodCollection`）とルール（`dayCol / weekCol`）で**同じ計算**をしている。
  - ルールの `math.floor()` は小数を返す（`string()` すると `2960.0`）。整数にしたいときは `int()` で囲む（週ランキングが登録できない不具合の原因だった）。
    ルールを変えたら `tests/rules` のテスト（エミュレーター）で必ず確認する。
- **成績の検証**：登録時にシード値と取引記録（`log`：`銘柄,売買,数量,建て時刻,建値,決済時刻,決済値,スワップ;…`）を保存。
  ランキングの「検証」ボタンで `verifyRound()` が相場を作り直し、各約定価格がその時点の相場と一致するか（許容差 スプレッド×4.5＋1ティック、時刻±800ms）、損益の合計が最終資産と合うかを判定する。
- **CSP**：`script-src 'self' https://www.gstatic.com https://www.google.com`、`connect-src 'self' https://*.googleapis.com …`、`img-src` に `https://www.google.com`（Firestore が通信失敗時に接続確認で `cleardot.gif` を読むため）。
  外部スクリプトを増やす場合や App Check（reCAPTCHA）を入れる場合は CSP も確認すること（reCAPTCHA 用のドメインは許可済み）。
- **Firebase SDK**：`https://www.gstatic.com/firebasejs/12.19.0/` から読み込む（app / auth / firestore / app-check は**必ず同じバージョン**にそろえる。混在すると Firestore が使えなくなる）。
- **外部データの表示**：ランキング・挑戦状URL・ニックネームは他人が書き換えられる値。表示前に必ずエスケープ（`escapeHtml`）または `textContent` を使い、数値は `Number()` で変換してから使う。

## 5. 未完了タスク

### 5-1. 利用者が手作業でやること（依頼があれば手順を案内する）
1. **【最優先】コミットに実メールアドレスが残っている問題**
   - GitHub の Settings → Emails で「Keep my email addresses private」「Block command line pushes that expose my email」を有効化
   - 既存のコミットにはGmailアドレスが残っているため、リポジトリを作り直す（同名で作れば公開URLもFirebase設定もそのまま）か、Claude Code で履歴を書き換えて force push する
2. **最新ファイルのアップロード**：CSP 対応で `index.html` を更新し、`app.js`・`ranking.js` を新規追加した（未反映なら反映が必要）
3. **Firebase の設定**
   - Authentication → Sign-in method で「匿名」を有効化
   - Authentication → 設定 → 承認済みドメインに `rinpei135.github.io` を追加
   - Firestore のルールを `firestore.rules` の内容に差し替えて公開
4. **公開後の安全対策**
   - Google Cloud コンソールで API キーの「アプリケーションの制限」を「ウェブサイト」にし、`https://rinpei135.github.io/*` のみ許可
   - （任意）App Check：reCAPTCHA v3 のサイトキーを取得し、`ranking.js` の `RECAPTCHA_V3_SITE_KEY` に設定

### 5-2. Claude Code にお願いしたいこと
1. **本番サイトでの動作確認**（claude.ai のチャット環境からは github.io と Firebase に接続できず、未確認）
   - 公開URLを実際に開き、タイムアタック → ランキング登録 → 3つの期間タブ表示 → 「検証」で「✓ 本物」になるか
   - 挑戦状URLを別のブラウザコンテキストで開き、同じ相場で勝負できるか
   - ブラウザのコンソールに CSP 違反やエラーが出ていないか
   - **注意**：本番のランキングに書き込むときは分かりやすいテスト用ニックネームを使い、確認後に Firebase コンソールで削除するよう利用者に伝える
   - `tests/e2e.py` はテスト用ビルド（参照口の追加・Firebase のモック化）が前提なので、本番サイトにはそのまま使えない。本番用には別途、軽いスモークテストを書くこと
2. **Firestore ルールの検証**：`dayCol()`・`weekCol()`（日本時間の日付・週の計算）と、損益率と資産額の一致チェックは実機で未確認。
   Firebase Emulator（`firebase-tools`）＋ `@firebase/rules-unit-testing` でルールの単体テストを書いて確認するのが望ましい
3. 上記で問題が見つかったら修正し、`tests/run_tests.sh` が全て通ることを確認してから反映する

## 6. テスト

```bash
pip install playwright
python3 -m playwright install chromium
(cd tests/rules && npm install)   # 任意：ルール・ランキングのテスト用（Node.js と Java が必要）
bash tests/run_tests.sh      # リポジトリ直下で実行
python3 tests/smoke_prod.py  # 本番サイトの確認（読み取りのみ）。--write で本番ランキングに書き込む通し確認
```

- `tests/prepare_test.py`：公開ファイルを `.test-build/` にコピーし、テスト用の参照口を追加、`ranking.js` を Firebase を使わないモックに差し替える（本番ランキングを汚さない）
- `tests/e2e.py`：PC・スマホの総合テスト 45 項目（初回導線、取引・注文、チャート操作、未来視点、株・板、裏タブ復帰、タイムアタック、ランキング登録データ、成績画像、検証の本物/改ざん判定、挑戦状の相場再現・勝敗、スマホ操作、ダークモード、CSP 違反の検出）
- `tests/xss.py`：攻撃文字列 10 種 × 3 か所（挑戦状URL・ニックネーム・ランキング）でスクリプトが実行されないことを確認
- `tests/rules/rules.test.mjs`：`firestore.rules` の単体テスト 9 項目（Firestore エミュレーター＋`@firebase/rules-unit-testing`）。コレクション名は `ranking.js` の `periodCollection()` をそのまま取り出して使い、ルールとの一致を確認
- `tests/ranking_emulator.py`：本物の `ranking.js` を Auth・Firestore エミュレーターにつなぎ、タイムアタック → 3期間に登録 → 各タブで自分の行を「検証」✓ まで通しで確認（gstatic の SDK は npm の `firebase` パッケージの同じバージョンのファイルで代用）
- `tests/smoke_prod.py`：本番サイト用。テスト用の参照口なしで公開ページを操作する。`--write` は「SMOKE_削除してOK」という名前で本番ランキングに書き込むので、確認後に Firebase コンソールで削除する
- `app.js` の `let lastRound = null;`・`const engines = {};`・`function currentM() {` の3か所はテストの参照口の差し込み位置なので、書き換える場合は `prepare_test.py` も合わせて直す

## 7. 直近の作業ログ（claude.ai チャットでの最終セッション）

「実際にホームページにアクセスして、バグがないかテストできますか？」以降に行ったこと：

1. **公開ファイルの確認**：GitHub から公開中の7ファイルを取得し、最新版と完全一致を確認
2. **総合テスト（45項目）**を作成・実行 → 次を発見して修正
   - **バグ**：挑戦状URLから初めて来た人の画面で、「遊び方」の説明が挑戦状の画面の裏に隠れて閉じられず、挑戦開始後に表に出てくる → `#helpModal` の重なり順を修正（ニックネーム → 遊び方 → 挑戦状の順）
   - **表示**：「成績を シェア」の不要な隙間、未使用時も「未来視点 ON」と表示されて紛らわしい → 「未来視点を使う」に変更
3. **セキュリティ確認**
   - リポジトリに秘密情報なし（Firebase の設定値は公開前提の値）
   - コミットの作者情報に実メールアドレスが公開されていることを発見（→ 5-1 の1）
   - XSS：攻撃文字列 10 種 × 3 か所で実行されないことを確認
   - Firestore ルールを見直し、「損益率と資産額の一致」チェックを追加（`firestore.rules`）
   - 多重防御として CSP を追加。そのためにインラインスクリプトを `app.js`・`ranking.js` に分離。CSP 有効状態で全テスト合格・違反なしを確認
4. **仕組み上残るリスク（利用者に説明済み）**：上限以内のもっともらしい成績の偽装（検証ボタンで見抜いて削除する運用）、匿名ID作り直しによる複数登録・大量アクセス（App Check で軽減）、不適切なニックネーム（削除で対応）

## 8. これまでの主な経緯（参考）

ドル円の遅延チャート案から始まり、TradingView 風の画面、ローソク足・描画ツール、架空企業「トキワープHD」の追加、
実在の証券コード（4649）との重複回避のため `TKWP` に変更、架空通貨「クロノ」への変更、未来情報を注文パネルに出さない方針、
未来視点の時間制限、Xシェア・成績カード画像、Firebase ランキング、挑戦状URL（シード値で同じ相場を再現）、
5分タイムアタック、匿名ログイン＋1人1件、成績の検証、期間別ランキング、効果音・振動、PWA、OGP、プライバシーポリシー、
軍資金100万円・上限1,000万円への変更、GitHub Pages での公開、の順で開発した。

広告（AdSense）は独自ドメインと審査が必要なため保留中（`index.html` に広告枠のサンプルだけ配置済み）。

## 9. Claude Code での作業ログ

1. **CSP 版の反映**：リポジトリには CSP 対応前の `index.html`（インラインスクリプト版）しかなかったため、インラインスクリプトを `app.js`・`ranking.js` に分離し、CSP の `<meta>` を追加。
   あわせて 7-2 の修正（遊び方の重なり順、「成績を シェア」の隙間、「未来視点を使う」の表記）を反映
2. **不具合修正**：`firestore.rules` の `weekCol()` が `ta5w_2960.0` を返し、**今週のランキングに一切登録できなかった**（画面には「前回の登録から60秒以上あけてください」と誤った案内が出る）→ `int()` で修正
3. **テストの弱点修正**：`e2e.py` の「挑戦状：同じ相場の再現」が比較点0件で素通りしていた（ティックは直近約100秒分しか保持しないため）→ ラウンド中に控えた相場と比較し、50点以上の一致を必須に
4. **テスト追加**：`tests/rules/`（ルール単体テスト・本物の ranking.js の通しテスト）、`tests/smoke_prod.py`（本番用スモークテスト）
5. **未確認**：Claude Code の実行環境のネットワーク制限で `rinpei135.github.io`・`www.gstatic.com` に接続できず、本番サイトでの確認（`smoke_prod.py`）は未実施
